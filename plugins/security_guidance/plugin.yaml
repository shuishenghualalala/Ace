name: security_guidance
version: "0.1.0"
description: "Warn when file writes or patches contain common unsafe code patterns."
author: "Crew"
kind: standalone
provides_hooks:
  - pre_tool_call
  - transform_tool_result
